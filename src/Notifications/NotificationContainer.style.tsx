import styled from "styled-components";

const Container = styled.div`
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;

  z-index: 1500;
`;

export const NotificationContainerSC = {
  Container,
};
